import { SecondLoginResponse } from './second-login-response.model';

describe('SecondLoginResponse', () => {
  it('should create an instance', () => {
    expect(new SecondLoginResponse()).toBeTruthy();
  });
});
