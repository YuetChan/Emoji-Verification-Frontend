import { AWSFaceDetectTestRequest } from './awsface-detect-test-request.model';

describe('AWSFaceDetectTestRequest', () => {
  it('should create an instance', () => {
    expect(new AWSFaceDetectTestRequest()).toBeTruthy();
  });
});
