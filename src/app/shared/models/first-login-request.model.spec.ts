import { FirstLoginRequest } from './first-login-request.model';

describe('FirstLoginRequest', () => {
  it('should create an instance', () => {
    expect(new FirstLoginRequest()).toBeTruthy();
  });
});
