import { SecondLoginRequest } from './second-login-request.model';

describe('SecondLoginRequest', () => {
  it('should create an instance', () => {
    expect(new SecondLoginRequest()).toBeTruthy();
  });
});
