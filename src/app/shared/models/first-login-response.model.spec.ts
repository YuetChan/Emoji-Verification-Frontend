import { FirstLoginResponse } from './first-login-response.model';

describe('FirstLoginResponse', () => {
  it('should create an instance', () => {
    expect(new FirstLoginResponse()).toBeTruthy();
  });
});
